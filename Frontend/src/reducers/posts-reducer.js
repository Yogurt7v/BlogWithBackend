const initialPostsState = {
    // user: null,
    // users: [],
    // posts: [],
    // post: null
};
export const postsReducer = (state = initialPostsState, action) => {
    switch (action.type) {
        default: {
            return state;
        }
    }
}